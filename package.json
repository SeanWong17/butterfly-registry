{
  "name": "butterfly-registry",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "description": "A digital specimen register for 58 protected butterfly catalog entries.",
  "scripts": {
    "dev": "python3 -m http.server 4173",
    "generate:thumbnails": "bash scripts/generate-thumbnails.sh",
    "check": "node --check theme-init.js && node --check data.js && node --check catalog.js && node --check i18n.js && node --check app.js",
    "test": "node --test tests/*.test.js",
    "test:e2e": "node tests/e2e-check.mjs"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
